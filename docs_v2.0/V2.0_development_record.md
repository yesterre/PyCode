V2.0_development_record.md 这个文档用于记录在v2.0开发中各个阶段的开发细节。在每个阶段开发时，需要包括以下内容：
1. 该阶段的计划开发内容；
2. 开发中是否出现了哪些问题，出现的问题及解决方法需要记录；
3. 该阶段开发结束实际完成情况的记录，同时也要给出该阶段的测试方法（命令行或测试步骤等），供我自行测试。

测试完成后，请按照以下要求撰写测试记录部分，测试报告不需要每次写这么长：
完成开发任务后，测试报告只需要给出：

1. 实际执行的测试命令
2. passed / failed / skipped 数量
3. 如果有失败，说明失败原因
4. 需要用户手动复测的最少步骤

在每个阶段开发过程中，需要按照上述要求及时跟进补充该文档内容。

# Phase 1 — 稳定 PyCode Core

日期：2026-09-08。状态：已完成（自动回归与离线手动示例通过，用户可按第 4 节自行复测）。依据：`V2.0_DEVELOPMENT_PLAN.md` Phase 1。

## 1. 计划开发内容

- 检查 Scanner、Parser、GraphBuilder、Retriever、Query、Runtime、Tools、Trace、Memory、Storage 的职责、输入输出、异常和副作用。
- 新增无运行状态的 `PyCodeEngine` 与结构化 `AnswerResult`，提供索引、图谱、查询、问答、解释、阅读顺序、影响分析和 Agent Python API。
- CLI 复用 Engine，保留函数签名、返回值、命令输出和错误语义；不改 Streamlit 页面。
- 保留既有 Agent 权限和降级机制，不改 planner 契约，不引入后端或基础设施依赖。
- 修改前基线、定向回归、完整回归；补充 README 和用户可复制的复测步骤。

验收：不导入 CLI／Streamlit 即可调用 Core；CLI 兼容；新旧测试通过；副作用和遗留事项明确记录。

## 2. 问题与解决方法

### P1-01：Windows 沙箱临时目录初始化失败（已处理）

修改前直接运行完整测试，使用 `tmp_path` 的测试出现 setup error；单独使用新目录运行 Scanner 测试仍出现同类错误，错误报告未能正常结束，两次执行手动中止。未取得完整错误堆栈或有效汇总，不能据此认定业务失败。

经授权在沙箱外、使用新的临时目录重跑，205 项现有测试全部通过（17.91 秒）。这是 CLI 迁移前的兼容基线；此时新增 Engine 和包导出已存在，但 CLI 尚未接入。后续测试同样在沙箱外运行，不引入 pytest 包装器或修改项目权限逻辑。

手动示例在沙箱内也出现明确 `PermissionError: [WinError 5]`，无法访问 `TemporaryDirectory` 刚创建的副本目录，清理同样被拒绝；授权在沙箱外执行相同示例后通过。首次失败留下的目录位于忽略的 `.pytest_tmp_v2_phase1_manual_demo`，未清理其他已有测试目录。

### P1-02：CLI 混合编排和展示（已迁移并通过回归）

原索引、图谱、问答和 Agent 命令同时承担调用与打印。新增 Engine 集中编排，CLI 只渲染返回结果；问答通过 `AnswerResult` 保留检索对象和证据，CLI 仍返回字符串。Artifact 错误提示保持兼容。

### P1-03：两种路径语义必须分别保留（已通过回归）

显式索引／图谱输出和普通 query 的 graph 路径沿用当前工作目录语义；Agent graph 路径按“绝对路径 → 当前工作目录下已有文件 → 项目内已有文件 → 原路径”解析。解析移至 Engine，不统一改为项目相对路径；工具仍执行项目内路径权限检查。

### P1-04：planner 副作用与内部状态写入（本阶段保留）

`plan_task()` 会更新传入 `AgentTask.task_type`。Engine 复用 `run_agent_task()` 每次创建任务，避免共享调用状态，不变更 planner 契约。Memory／Task DAG 可能写入 `.pclens`；关闭自动 Memory 提取不等于禁止全部内部状态工具。默认不运行测试、不改源代码、不提交 Git 的边界保持不变。

### P1-05：新测试对现有 Agent 行为的两个假设不准确（已修正并通过复测）

首次定向测试 43 passed / 2 failed：第二个隔离项目只有顶层 print，没有静态入口线索，因此不能要求入口检索返回非空证据；未授权的 `run_tests` 在 LLM action 校验阶段就被拒绝，不一定进入 Executor 并生成 ToolResult。

修正测试项目以包含独立入口；分别验证 `run_tests` 的规划阶段拒绝和一般非只读工具的 Executor policy 拒绝。保留对不执行工具、fallback Trace、授权参数传递的断言，不修改 Runtime／Policy 语义。

第二次定向测试 46 passed / 1 failed：在 `other.py` 中增加 main guard 仍不足以匹配现有 Query 的入口启发式，该查询按入口文件名或名为 main 的函数识别。最终将隔离项目入口命名为 `app.py`，保持不同项目使用不同文件名以检查证据不混用。此限制属于既有静态查询能力，不在 Phase 1 扩展。

### 模块职责检查结果

| 模块 | 输入／输出与职责 | 副作用／异常及处理决定 |
| --- | --- | --- |
| Scanner / Parser | 项目路径 → Python 文件 → FileInfo | 只读；保留路径、读取和 SyntaxError 异常 |
| GraphBuilder / Query | ProjectIndex → CodeGraph；图谱 → 节点／边 | 内存计算；不加入数据库或 HTTP 逻辑 |
| Retriever | 问题／目标 + index / graph → RetrievalResult | 读取有限源码上下文，保留现有算法 |
| Engine | 编排上述能力，返回领域对象 | index / graph 写产物；问答可能调用 LLM；不打印 |
| Runtime / Planner | 新 AgentTask + 配置 → AgentResult | 每次构造运行状态，沿用规划、观察、停止与降级语义 |
| Executor / Tools / Policy | 工具调用 → ToolResult | 统一参数校验、项目路径和测试授权；不绕过权限 |
| Trace / Context / Todo | 事件、证据和运行进度 | 保留现有结构，不提前实现持久化事件协议 |
| Memory / Task DAG | 项目辅助知识与跨会话任务 | 本地内部状态读写；不作为后台调度系统 |
| Storage | 领域对象 ↔ JSON 文件 | 创建父目录、写入产物；保留原格式和读取异常 |

## 3. 实际完成情况

| 计划项 | 实际交付 | 状态 |
| --- | --- | --- |
| 核心职责检查 | 第 2 节职责表、路径约定、planner 副作用和权限边界 | 完成 |
| 独立 Python API | `pycode/engine.py` 的 9 个方法，`AnswerResult`，包根导出 | 完成 |
| CLI 迁移 | index / graph / query / ask / explain / onboard / impact / agent 调用 Engine；保留旧返回类型、build_project_index 包装和展示 | 完成 |
| API 行为测试 | `tests/test_engine.py` 共 20 项，含独立进程导入隔离、路径、证据、状态隔离、权限、模型异常及 fallback | 完成 |
| 回归 | 定向 47 passed；最终完整 225 passed，0 failed / 0 skipped | 完成 |
| 文档与复测 | README Python API 示例；本节问题记录与第 4 节精简测试记录及复测命令，离线 smoke 已实际通过 | 完成 |

公开调用：`from pycode import PyCodeEngine, AnswerResult`。Engine 构造无 IO，无跨调用项目状态；`build_index` 不写文件，`index/graph` 分别保存产物，问答返回答案和检索对象，Agent 返回现有 AgentResult。显式 `llm_client` 优先于 model，包含可定义为 falsey 的客户端对象；`explain/impact` 文件目标同时接受 str 与 Path。

计划偏差：修改前沙箱基线未得到有效汇总，205 项兼容基线在新增独立 Engine 后、CLI 接入前取得；未修改 pytest 配置。测试中两个假设经实际代码核对修正，未扩大到算法或 Runtime 重构。

遗留／范围：保留 planner 修改 task_type 的内部契约、入口启发式及 JSON 存储方式；不支持同项目并发写入协调，未引入 Snapshot／Backend／Worker／事件持久化；真实模型接口及回答质量未在本阶段联网验收。总计划 Current Phase 保持 Phase 1，未自动推进 Phase 2。

## 4. 测试记录

### 4.1 实际执行的测试命令

2026-09-08，在项目根目录使用虚拟环境执行，以下为最终定向与完整回归命令（经授权在 Windows 沙箱外运行）：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_engine.py tests/test_cli.py tests/test_v1_acceptance.py --basetemp=.pytest_tmp_v2_phase1_targeted_final -o cache_dir=.pytest_tmp_v2_phase1_targeted_final/.pytest_cache --tb=short
.\.venv\Scripts\python.exe -m pytest tests --basetemp=.pytest_tmp_v2_phase1_full -o cache_dir=.pytest_tmp_v2_phase1_full/.pytest_cache --tb=short
```

### 4.2 测试结果

| 验证 | passed | failed | skipped |
| --- | ---: | ---: | ---: |
| 最终定向回归 | 47 | 0 | 0 |
| 最终完整回归 | 225 | 0 | 0 |

以上为开发完成时的结果；2026-09-09 仅精简文档，未重新执行测试。离线 API／CLI 示例此前已通过，输出 `PHASE1_SMOKE_OK`；真实模型未联网验收。

### 4.3 失败原因

最终回归无失败。开发中曾遇到：

- Windows 沙箱临时目录访问被拒绝，执行中止、无有效统计；授权在沙箱外重跑通过（见 P1-01）。
- 前两轮定向测试分别为 43 / 2 / 0、46 / 1 / 0（passed / failed / skipped）：入口样例不符合既有查询规则，以及测试误以为未授权 `run_tests` 一定进入 Executor。修正样例和断言后通过，未修改 Agent 语义（见 P1-05）。

### 4.4 用户手动复测的最少步骤

在项目根目录 PowerShell 执行下面一条完整回归命令即可，无需 API Key：

```powershell
.\.venv\Scripts\python.exe -m pytest tests --basetemp=.pytest_tmp_v2_phase1_manual_full -o cache_dir=.pytest_tmp_v2_phase1_manual_full/.pytest_cache --tb=short
```

预期 `225 passed`；若 PATH 中没有 Git，Git diff 验收场景会跳过。临时目录须专用于测试；遇到权限错误时换新目录名并同步修改 `cache_dir`，必要时在普通本地终端重试。

# Phase 2 — FastAPI Backend

日期：2026-09-09。状态：已完成（Git Repository Source 补充、Backend 定向回归和完整回归通过；3 项真实符号链接用例受 Windows 权限限制跳过）。

## 1. 计划开发内容

- 建立 Router → Application Service → PyCodeAdapter → PyCodeEngine 分层，同步执行六个接口：health、创建／查询项目、index、ask、impact。
- 对外 Project 契约使用 `name`、`repo_url`、可选 `branch` 和状态；创建只登记，不 Clone，允许相同 Repository 创建多个 Project，服务器绝对路径不进入响应。
- index 在 `<workspace_root>/<project_id>/repo` 准备源码：缺少仓库时 Clone，存在时复用且不自动 pull；然后通过现有 Adapter 调用 Core 生成 Index 和 Code Graph。
- 使用应用内存 Store，单进程同步请求；状态支持 created → preparing → indexing → ready，任一步失败进入 failed，并保留安全错误信息和重试能力。
- Git ingestion 只允许配置 Host 上无内嵌认证的 HTTPS URL，关闭交互、隔离 Git 配置、限制协议和重定向并设置 timeout；不递归 submodule，不运行仓库代码、安装、脚本或测试。
- Clone checkout 前拒绝 Git tree 中的符号链接、submodule 和 `.pclens`，落盘后验证 Workspace 文件边界；新增离线真实 Git 与假 LLM HTTP 测试。
- 保持 Router → Service → Adapter → Core 分层，不修改无关 Core；不实现 PostgreSQL、ProjectSnapshot、Redis、Worker、自动远程更新、SSE 或前端。

## 2. 问题与解决方法

- **依赖安装权限（已处理）**：沙箱阻止 pip 写入临时构建目录；授权在沙箱外重试后安装成功，未修改项目权限或引入包装器。
- **环境依赖与声明不一致（已校正并通过完整回归）**：原环境 OpenAI SDK 为 2.41.1，与项目既有 `<2.0` 声明不符；安装时解析为 1.109.1。保留原依赖约束。实际版本：FastAPI 0.141.1、Starlette 1.3.1、Pydantic 2.13.4、Uvicorn 0.50.2、HTTPX 0.28.1；`pip check` 已通过。
- **导入隔离测试误把测试工具算作应用依赖（已修正并通过复测）**：HTTPX 的可选 CLI 导入 Rich，导致禁止展示依赖的子进程测试失败；改为隔离验证应用创建和 OpenAPI，HTTP 行为仍由真实 TestClient 测试覆盖，不改变后端架构。
- **Windows 符号链接权限限制**：3 项真实符号链接测试因 OS 不允许创建链接而跳过；另加不依赖系统特权的路径解析越界测试，验证 impact 和 Artifact IO 在解析出项目外路径时拒绝执行。
- **测试客户端弃用提示**：Starlette 1.3.1 提示未来迁移到 HTTPX2；当前 HTTPX 测试可运行。本阶段按计划保留 HTTPX，不隐藏警告或增加未经需要的依赖迁移。
- **Git checkout 的隐藏执行面（已处理）**：仅使用 `shell=False` 仍可能继承用户 Git 配置、credential helper、hooks、fsmonitor 或 smudge filter。实现改为隔离系统／全局配置和环境变量，关闭交互与 hooks，禁止重定向和非 HTTPS protocol，跳过 LFS smudge，并在 checkout 前检查 tree。离线测试注入恶意 filter 配置和仓库脚本，确认均未执行。
- **Windows 可能把 Git symlink 落为普通文件（已处理）**：不能只依赖文件系统 `is_symlink()`。Clone 先 `--no-checkout`，通过 `git ls-tree` 的 mode 拒绝符号链接和 submodule，再 checkout；该测试不需要 Windows 创建链接权限并已通过。
- **修改前测试的沙箱临时目录权限（已处理）**：首次在沙箱中执行出现大量 setup error，未计为业务测试结果；使用新的 pytest 临时目录在沙箱外重跑，获得 292 passed / 0 failed / 3 skipped 的有效基线。没有修改业务语义掩盖环境问题。

## 3. 实际完成情况

| 计划项 | 实际完成情况 |
| --- | --- |
| Backend 分层 | 保留 Router、Schema、Service、PyCodeAdapter、PyCodeEngine；Repository 获取和 UUID Workspace 位于 Backend infrastructure，Core 无 Git／FastAPI 依赖 |
| Project API | 创建／查询响应使用 `repo_url`、可选 `branch`，不接受或暴露 `local_path`；创建无磁盘或网络副作用，同源 Project 彼此独立 |
| Index 流程 | 首次准备临时目录并浅 Clone、检查 tree 和落盘边界、原子移入 Project Workspace，再建立 Index / Graph；已有工作区直接重建产物且不 pull |
| 状态与并发 | created / preparing / indexing / ready / failed；安全错误、失败重试、同项目操作锁、不同项目独立处理；GET／health 不等待操作锁 |
| 安全与异常 | HTTPS Host／URL／branch 校验、Git 非交互和 timeout、配置隔离、拒绝 symlink／submodule／保留产物；缺失产物、路径、模型及 Git 错误集中映射并清理新建 staging |
| 测试 | 113 项 Backend 用例，含临时 Git 仓库全链路且不依赖公共网络；最终 Backend 110 passed / 0 failed / 3 skipped，完整回归 335 passed / 0 failed / 3 skipped |
| 文档 | 总计划和 README 将 Git URL 定义为 v2 主要 Repository Source，记录配置、状态、安全边界、错误语义及最少复测步骤 |

实现补充：问答前使用 Core Storage 校验 Artifact，避免把模型错误误判为产物损坏；损坏时将项目标记 failed，要求重新索引。项目记录使用不可变对象，状态更新受锁保护，防止 GET 读到部分更新。Artifact 的解析路径同样检查项目边界，防止已有链接把写入转移到项目外；未修改 Core 代码。

当前仍为单进程、内存登记和同步请求。服务重启后需新建 Project；旧 Workspace 文件保留但不自动认领或删除。Phase 2 不自动 pull，不提供 Commit / Snapshot 一致性、数据库、Redis、Worker、AgentRun、SSE 或前端。真实模型服务和公共网络 Clone 未作为自动验收条件；离线 Git 仓库与假 LLM 已覆盖 HTTP 全链路。总计划 Current Phase 保持 Phase 2，未进入 Phase 3。

## 4. 测试记录

### 4.1 实际执行的测试命令

2026-09-09，在项目根目录执行（pytest 在沙箱外）：

```powershell
.\.venv\Scripts\python.exe -m pip check
.\.venv\Scripts\python.exe -m pytest tests --basetemp=.pytest_tmp_v2_source_baseline_verified -o cache_dir=.pytest_tmp_v2_source_baseline_verified/.pytest_cache --tb=short -rs
.\.venv\Scripts\python.exe -m pytest tests/backend --basetemp=.pytest_tmp_v2_source_targeted_final -o cache_dir=.pytest_tmp_v2_source_targeted_final/.pytest_cache --tb=short -rs
.\.venv\Scripts\python.exe -m pytest tests --basetemp=.pytest_tmp_v2_source_full_final -o cache_dir=.pytest_tmp_v2_source_full_final/.pytest_cache --tb=short -rs
.\.venv\Scripts\python.exe -m compileall -q backend tests/backend
```

### 4.2 测试结果

| 验证 | passed | failed | skipped |
| --- | ---: | ---: | ---: |
| Repository Source 修改前完整基线 | 292 | 0 | 3 |
| 最终 Backend 定向（含离线 Git） | 110 | 0 | 3 |
| 最终完整回归 | 335 | 0 | 3 |

`pip check` 无冲突；`compileall` 成功。自动测试通过真实 HTTP → Service → RepositoryWorkspace → Git → Adapter → Core 链路，使用临时本地 Git 仓库映射测试 URL，无需模型配置或公共网络。真实公共仓库 Clone 和真实模型调用未执行，不标为通过。

### 4.3 失败与跳过原因

最终无失败。3 项跳过均为 Windows `WinError 1314`，缺少创建真实符号链接的权限；Git tree mode 的 symlink／submodule 拒绝以及无需特权的解析越界测试已通过。另有 1 条 Starlette 建议迁移 HTTPX2 的弃用提示，不影响结果。

### 4.4 用户手动复测的最少步骤

在项目根目录执行一次完整回归，无需模型配置：

```powershell
.\.venv\Scripts\python.exe -m pytest tests --basetemp=.pytest_tmp_v2_phase2_manual -o cache_dir=.pytest_tmp_v2_phase2_manual/.pytest_cache --tb=short -rs
```

这条命令已经覆盖不依赖公共网络的 Git Clone、Index、Graph、Ask 和 Impact 全链路。最少人工检查是启动 `.\.venv\Scripts\python.exe -m uvicorn backend.app.main:app --host 127.0.0.1 --port 8000`，打开 `http://127.0.0.1:8000/docs`：

1. 调用创建接口，提交 `{"name":"Flask","repo_url":"https://github.com/pallets/flask.git"}`，确认立即返回 created，响应无服务器路径。
2. 使用返回的 ID 调用 index，确认依次出现 preparing／indexing（小仓库可能很快完成）并最终 ready；首次操作需要 Git 和网络，会写入 `.pycode-workspaces/<id>/repo`。
3. 再次调用 index，确认仍 ready 且复用当前工作区；Phase 2 不会 pull。已配置真实模型时才手动调用 ask／impact。

### 人工验收结果

**Phase 2 状态：已完成**

人工验收项目：

- [x] Health 健康检查接口正常
- [x] Project 创建与查询正常
- [x] 支持通过 Git Repository URL 创建项目
- [x] Repository 能够正确 Clone 至受控 Workspace
- [x] 不同 Project 使用独立 Workspace
- [x] Index 与 Code Graph 能够正常生成
- [x] 重复执行 Index 能够正确复用现有 Workspace
- [x] Repository 获取失败时能够正确处理并清理临时工作区
- [x] 非法 Repository Source 能够被正确拒绝
- [x] 完整 pytest 回归测试通过，无回归失败

**结论：Phase 2 的 FastAPI Backend 与 Git Repository ingestion 主链路已完成验收，可以进入 Phase 3 — PostgreSQL 持久化开发。**

# Phase 3 — PostgreSQL 持久化

日期：2026-09-10。状态：已完成（开发／测试数据库 Migration、真实 PostgreSQL 集成、Backend 定向和完整回归通过；3 项真实符号链接用例受 Windows 权限限制跳过）。依据：`V2.0_DEVELOPMENT_PLAN.md` Phase 3 与已确认的 Phase 3 A～G 架构审查方案。

## 1. 计划开发内容

- 使用同步 SQLAlchemy 2.x 与 Psycopg 3 接入 PostgreSQL；生产读取 `DATABASE_URL`，真实数据库测试只读取独立的 `TEST_DATABASE_URL`。
- 建立 `projects`、`project_snapshots`、`agent_runs`、`trace_events` 四张 ORM 表，并通过 Alembic Initial Migration 创建 Schema，不使用 `Base.metadata.create_all()`。
- 将生产 Project 持久化从应用内存切换为 PostgreSQL；保留内存 Repository 作为现有离线 API/Git 测试替身。
- 将项目级进程内操作锁从旧 Store 拆出，保持同项目互斥语义，但不引入 Redis 或分布式锁。
- 成功 index 后安全读取本地 Git HEAD，按 `(project_id, commit_sha)` upsert Snapshot；Project API 的 `index_summary` 从最新 ready Snapshot 组装。
- 通过持久化 `workspace_path` 与既有 Filesystem 安全校验恢复重启后的 Workspace，不自动认领无数据库 Project 的遗留目录。
- 在现有 ask/impact 链路中记录 running → completed/failed AgentRun；不新增 AgentRun API，不伪造 token，不创造虚假 TraceEvent。
- 增加真实 PostgreSQL 的 UUID、FK、UNIQUE、JSONB、TIMESTAMPTZ、rollback、Migration 和 App restart 测试；测试清理严格拒绝 `pycode_dev`。
- 完成 Backend 定向回归、数据库集成、Migration 验证和完整 pytest 回归，并更新 README、开发计划和本记录。

## 2. 问题与解决方法

- **依赖安装的 Windows 沙箱临时目录权限（已处理）**：沙箱内 pip 在系统临时 build tracker 目录收到 `Permission denied`；按既有处理规则，经单次授权在沙箱外安装成功。未修改生产权限或绕过安全边界。
- **Alembic CHECK Constraint 命名重复（已处理）**：首次离线 DDL 检查发现手写 Migration 的完整 CHECK 名称被 naming convention 再次加前缀。Migration 改用 `op.f(...)` 固化名称，复查后得到预期的 `ck_*` 名称。
- **Git 复用测试未计入 Phase 3 的只读 HEAD 调用（已处理）**：原测试用 Git 子进程总数判断没有远程更新；Snapshot 最小持久化合法增加一次 `rev-parse`。断言改为 Clone 次数不增加且远端新增文件不出现，仍验证没有 fetch/pull/重新 Clone。
- **pytest 沙箱临时目录权限（已处理）**：首次 Service 定向测试出现大量 setup error 并在清理阶段阻塞；停止该无效执行后，使用新的专用目录并经单次授权在沙箱外重跑，69 passed / 0 failed / 3 skipped。
- **`.env` 存在但进程未自动加载（已处理）**：初次检查时当前进程没有 `DATABASE_URL` 或 `TEST_DATABASE_URL`，真实 PostgreSQL 测试按 Fail-Safe 明确跳过。用户确认两个变量已配置在 `.env` 后，引入 `python-dotenv` 并以 `override=False` 加载本地开发配置；全过程未输出连接 URL、用户名或密码。安全检查只确认开发／测试数据库名分别为 `pycode_dev`／`pycode_test`。
- **完整回归中的 Windows 临时目录 rename 权限波动（已处理）**：前两次完整回归均已完成 342 项通过，但各有一个不同 Backend fixture 在临时 Clone 原子 rename 时遇到随机 `WinError 5` setup error；对应单项和 Backend 全量均可通过。未修改生产 Workspace 逻辑，改用新的系统临时根复跑后取得 343 passed / 0 failed / 3 skipped 的有效完整结果。

## 3. 实际完成情况

| 计划项 | 实际完成情况 |
| --- | --- |
| 数据库基础 | 同步 SQLAlchemy 2.0.52、Psycopg 3.3.5、请求级 Session、应用级 Engine/sessionmaker；dotenv 不覆盖外部环境变量 |
| ORM / Schema | 独立 Backend ORM 建立 projects、project_snapshots、agent_runs、trace_events；UUID、TIMESTAMPTZ、JSONB、FK、CHECK、UNIQUE 和查询索引均由 PostgreSQL 验证 |
| Alembic | Initial Revision `20260910_0001` 创建四张业务表；`pycode_test` 从 base 升级成功，`pycode_dev` upgrade head 成功，`alembic check` 无差异 |
| Repository / 事务 | 四个明确 Repository 加薄 UnitOfWork；Repository flush，Service 决定短事务边界，异常 rollback，Session Dependency 最终 rollback／close |
| Project 持久化 | 生产 `create_app()` 默认 PostgreSQL；InMemoryPersistence 只由测试显式注入；相同 repo_url 可创建多个 Project |
| Snapshot / Summary | index 成功后通过隔离 Git 执行 `rev-parse --verify HEAD^{commit}`；按 Project + Commit upsert；API index_summary 来自最新 ready Snapshot |
| AgentRun / TraceEvent | ask／impact 最小持久化 running → completed/failed，关联当前 Snapshot，token 保持 NULL；TraceEvent Repository 支持 JSONB、sequence 唯一与排序，不创造虚假事件 |
| Workspace 重启 | 移除 `_prepared` 进程归属判断，以持久化 workspace_path、预期 UUID 路径、`.git` 和既有链接／边界检查共同恢复；双 App 测试通过 |
| 兼容与范围 | HTTP Request/Response 保持兼容，Git ingestion、错误清理和项目级进程锁保留；未修改 `pycode/`，未引入 Redis、Worker、SSE 或 Phase 4 生命周期 |
| 测试 | PostgreSQL 6 passed；最终 Backend 118 passed / 0 failed / 3 skipped；最终完整 343 passed / 0 failed / 3 skipped |

## 4. 测试记录

### 4.1 实际执行的测试命令

```powershell
.\.venv\Scripts\python.exe -m pytest tests/backend/test_postgresql.py --basetemp=.pytest_tmp_v2_phase3_postgresql_1 -o cache_dir=.pytest_tmp_v2_phase3_postgresql_1/.pytest_cache --tb=short -q -rs
.\.venv\Scripts\python.exe -m alembic upgrade head
.\.venv\Scripts\python.exe -m alembic current
.\.venv\Scripts\python.exe -m alembic check
.\.venv\Scripts\python.exe -m pytest tests/backend --basetemp=.pytest_tmp_v2_phase3_backend_final -o cache_dir=.pytest_tmp_v2_phase3_backend_final/.pytest_cache --tb=short -q -rs
.\.venv\Scripts\python.exe -m pytest tests --basetemp=C:\Users\hp-pc\AppData\Local\Temp\pycode_phase3_full_final_20260910_a -o cache_dir=C:\Users\hp-pc\AppData\Local\Temp\pycode_phase3_full_final_20260910_a\.pytest_cache --tb=short -q -rs
.\.venv\Scripts\python.exe -m pip check
.\.venv\Scripts\python.exe -m compileall -q backend tests/backend
```

### 4.2 测试结果

| 验证 | passed | failed | skipped |
| --- | ---: | ---: | ---: |
| PostgreSQL / Migration / Restart 定向 | 6 | 0 | 0 |
| 最终 Backend（含真实 PostgreSQL） | 118 | 0 | 3 |
| 最终完整回归 | 343 | 0 | 3 |

`pycode_dev` 当前 Revision 为 `20260910_0001 (head)`，四张业务表存在；`alembic check` 输出 `No new upgrade operations detected.`。`pip check` 无依赖冲突，`compileall` 成功。

### 4.3 失败与跳过原因

最终有效回归无失败。3 项跳过仍为 Windows `WinError 1314`，当前账户没有创建真实符号链接的权限；不依赖 OS 特权的 Git tree symlink/submodule 拒绝和解析越界测试均已通过。开发中的两次完整回归各遇到一个随机临时目录 `WinError 5` setup error，换用系统临时根后完整通过，未计为业务失败。

### 4.4 用户手动复测的最少步骤

确认 `.env` 中 `DATABASE_URL` 指向 `pycode_dev`、`TEST_DATABASE_URL` 指向独立 `pycode_test`，在项目根目录执行：

```powershell
.\.venv\Scripts\python.exe -m alembic upgrade head
.\.venv\Scripts\python.exe -m pytest tests/backend/test_postgresql.py --basetemp=.pytest_tmp_v2_phase3_manual_db -o cache_dir=.pytest_tmp_v2_phase3_manual_db/.pytest_cache --tb=short -q
.\.venv\Scripts\python.exe -m pytest tests --basetemp=.pytest_tmp_v2_phase3_manual_full -o cache_dir=.pytest_tmp_v2_phase3_manual_full/.pytest_cache --tb=short -rs
```

最少人工 HTTP 验收：启动 Uvicorn，创建 Project 并执行 index；重启 Uvicorn 后用原 Project ID GET，确认仍为 ready 且 `index_summary` 存在，再调用 ask／impact。Phase 3 不要求新增 AgentRun/TraceEvent HTTP API；其持久化由 PostgreSQL 自动测试覆盖。

## Phase 3 手动验收记录

Phase 3 PostgreSQL 持久化功能已完成手动验收。

验收内容：

- [x] Alembic Migration 正常，数据库 Revision 为最新版本
- [x] `pycode_dev` 中成功创建：
  - `projects`
  - `project_snapshots`
  - `agent_runs`
  - `trace_events`
- [x] 创建 Project 后，数据能够正确写入 PostgreSQL
- [x] Project 执行 Index 后状态更新为 `ready`
- [x] Index 成功后能够创建并持久化 ProjectSnapshot
- [x] Snapshot 中 `commit_sha` 与本地 Git HEAD 一致
- [x] `file_count / node_count / edge_count` 能够正确持久化
- [x] ProjectResponse 中的 `index_summary` 与 Snapshot 数据一致
- [x] 重启 FastAPI 后，原 Project 仍可正常查询
- [x] 重启后已有 Workspace 能够继续正常复用
- [x] 重复执行 Index 不会为相同 Commit 创建重复 Snapshot
- [x] `/ask` 成功执行后能够生成 `completed` AgentRun
- [x] `/impact` 成功执行后能够生成 `completed` AgentRun
- [x] 失败的 ask / impact 请求能够正确记录为 `failed` AgentRun
- [x] AgentRun 能够正确关联 Project 与 Snapshot
- [x] Project 失败状态和 `last_error` 能够持久化
- [x] PostgreSQL 持久化没有破坏 Phase 2 的 Git Repository ingestion、Index、Ask、Impact 主链路

补充说明：

- `trace_events` 当前阶段主要完成 Schema、Repository 和 PostgreSQL 测试能力，尚未接入完整实时 Agent Trace，因此人工验收时表为空属于正常情况。
- PostgreSQL 负责长期结构化业务状态；
- Repository 源码和 Index / Code Graph Artifact 仍保存在 Filesystem；
- Redis、Worker、SSE 和完整 Snapshot 生命周期留待后续阶段。

**结论：Phase 3 手动验收通过，可以进入下一阶段开发。**

# Phase 4 — Repository 与 Snapshot 生命周期

日期：2026-09-10～2026-09-11。状态：代码实现与自动化验收完成，等待人工验收；未进入 Phase 5。依据：`V2.0_DEVELOPMENT_PLAN.md` Phase 4、已确认的 A～K 设计，以及四项补充实施要求。

## 1. 计划开发内容

- 继续由 `POST /projects/{project_id}/index` 承担首次 Clone 和后续 Fetch／Resolve／必要时重新分析，不增加无必要的 Git／Snapshot HTTP API。
- 使用完整 branch 历史、已有 shallow Workspace 的一次性 unshallow、remote ref 精确解析、detached HEAD 与 Snapshot Git ref，保证分析绑定 exact Commit。
- 新增显式 `projects.current_snapshot_id`，使 Project 最近一次更新状态与当前可用分析版本分离；ask／impact 以 current ready Snapshot、有效 Artifact 和可用 Commit 为依据。
- Snapshot 使用 indexing／ready／failed 最小状态机；同一 Project + Commit 始终复用同一行，失败和 Artifact repair 更新原 Snapshot。
- 将 Index／Graph 保存到 Snapshot 独立 Artifact bundle；采用 Windows 可恢复的 staging／replacement 方案，不假设 `os.replace` 能覆盖非空目录。
- checkout 后和 Artifact 发布前分别校验 HEAD 等于 target Commit；只有两次校验均通过才能将 Snapshot 标记 ready。
- 兼容 Phase 3 数据和 `repo/.pclens` 旧 Artifact，采用受锁保护的 lazy relocation／repair，不要求清空 `pycode_dev`。
- 保持短数据库事务、ProjectOperationLocks、Phase 2 Git 安全环境和完整 Phase 2／3 API 回归；不提前实现 Redis、Worker、SSE 或 Commit Compare 产品功能。

## 2. 当前实施记录

### P4-01：Schema 与第二个 Alembic Revision（按要求生成并暂停）

已在不连接数据库的前提下建立 ORM／领域字段，并新增 Revision `20260910_0002`。该 Revision：

- 为 `projects` 增加 nullable `current_snapshot_id`、Snapshot FK（`ON DELETE SET NULL`）和索引；
- 为 `project_snapshots` 增加 nullable `error_message`；
- 将三个 count 调整为 nullable，以表达 indexing／failed 尚无完整 Summary；
- 增加 indexing／ready／failed 状态 CHECK；
- 增加 ready Snapshot 的 Artifact 路径、统计值完整且无 error 的 CHECK；
- 按 Phase 3 的 `updated_at DESC, id DESC` latest-ready 规则为旧 Project backfill current Snapshot；
- downgrade 先把 NULL count 转为 0，再恢复 Phase 3 的 non-null 约束。

根据阶段要求，Revision 生成后曾暂停人工检查；在获得确认前没有连接数据库或继续 Repository／Artifact 实施。历史 Revision `20260910_0001` 始终未修改。

### P4-02：Phase 3 历史数据 Migration 验证（pycode_test 已通过）

Migration 经人工审查批准后，只在安全检查确认数据库名为 `pycode_test` 且连接 URL 不等于 `DATABASE_URL` 时执行。测试没有 DROP DATABASE／DROP SCHEMA，也未连接或升级 `pycode_dev`。

测试从 Revision `20260910_0001` 构造一个 Project 和两个不同 `updated_at` 的 ready Snapshot，执行 `0001 → 0002 → 0001 → 0002`。验证结果：

- `current_snapshot_id` 每次均 backfill 到较新的 ready Snapshot；
- 两个旧 Snapshot 的 ID、Artifact path 和三个统计值在 upgrade／downgrade 后保持不变；
- 非法 Snapshot status 被 CHECK 拒绝；
- 缺少 Artifact／统计信息的 ready Snapshot 被 ready-complete CHECK 拒绝；
- count 和 Artifact 均为空的 indexing Snapshot 可以合法存在；
- 不存在的 current Snapshot 被数据库 FK 拒绝；
- SQLAlchemy 与内存 Repository 都拒绝把 Project.current_snapshot_id 指向其他 Project 的 Snapshot，失败事务不改变原 current Snapshot；
- downgrade 恢复 Phase 3 字段集合与 non-null count，随后重新 upgrade 能再次正确 backfill。

实际执行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/backend/test_api.py::test_memory_repository_rejects_current_snapshot_from_another_project --basetemp=.pytest_tmp_v2_phase4_current_memory -o cache_dir=.pytest_tmp_v2_phase4_current_memory/.pytest_cache --tb=short -q
.\.venv\Scripts\python.exe -m pytest tests/backend/test_phase4_migration.py --basetemp=C:\Users\hp-pc\AppData\Local\Temp\pycode_phase4_migration_20260910_a -o cache_dir=C:\Users\hp-pc\AppData\Local\Temp\pycode_phase4_migration_20260910_a/.pytest_cache --tb=short -q -rs
```

结果：内存 Repository 1 passed；PostgreSQL Migration／Repository 2 passed；0 failed／0 skipped。测试结束后 `pycode_test` 位于 `20260910_0002`，测试业务数据已清理。仅出现既有 Starlette HTTPX2 弃用提示和 Windows pytest cache `WinError 5` 警告，不影响测试结果。截至该检查点，`pycode_dev` 尚未执行 Phase 4 Migration。

### P4-03：开发数据库升级与 Schema 一致性（已完成）

在 Migration 人工审查、`pycode_test` 历史数据往返验证均通过，并收到继续实施授权后，执行 `alembic upgrade head` 将 `pycode_dev` 从 `20260910_0001` 升级到 `20260910_0002`。未执行 DROP DATABASE／DROP SCHEMA，也未要求清空既有业务数据。

最终复核：

```powershell
.\.venv\Scripts\python.exe -m alembic current
.\.venv\Scripts\python.exe -m alembic check
```

结果为 `20260910_0002 (head)`，并且 `No new upgrade operations detected.`，说明当前 ORM Metadata 与 Migration head 一致。

### P4-04：受管理 Repository 生命周期（已完成）

- `POST /projects/{id}/index` 保持单一入口；首次操作执行远程 branch 解析与完整 `--single-branch --no-checkout` Clone，后续操作执行 Fetch，不使用 `git pull`，也不产生 merge commit。
- `branch` 显式指定时验证并解析 `refs/heads/<branch>`；为空时通过远程 `HEAD` symref 解析默认 branch。只有 Git 明确返回 ref 不存在时才映射为 `repository_branch_missing`，连接类失败仍按安全的 Repository 错误返回。
- Fetch 使用显式 HTTPS URL 和受控 refspec 更新 `refs/remotes/origin/<branch>`；Phase 2 遗留 shallow Workspace 会先执行一次 `--unshallow`。新 Clone 不再使用 `--depth 1`。
- 远程 ref 先解析为不可变 exact Commit，再建立 `refs/pycode/snapshots/<snapshot_id>` 保护引用，使用 detached HEAD checkout；受控 Workspace 内执行强制 checkout 和 `git clean -ffdx`，Artifact 历史位于 Git tree 外，不会被清理。
- checkout 后立即校验一次 HEAD，Index／Graph 完成且准备发布 Artifact 前再次校验；任一不一致均拒绝把 Snapshot 标记 ready。
- 继续沿用非交互 Git、隔离配置、禁止凭据和重定向、命令超时、`shell=False`、tree 元数据校验、Workspace root／link／reparse point 边界；不初始化 submodule、不执行 hook、smudge filter 或 Repository 内代码。

### P4-05：Snapshot、Artifact 与分析版本语义（已完成）

- Snapshot 状态机为 `indexing → ready | failed`；Commit resolve 后、长耗时分析前创建或复用 `(project_id, commit_sha)` 唯一行。同 Commit 失败重试和 Artifact repair 更新原行，不生成重复 Snapshot。
- `projects.current_snapshot_id` 只允许由 Repository 指向同 Project 的 ready Snapshot。Project 操作状态与当前可用分析版本分离：新 B 失败时 Project 为 failed 并记录安全错误，但旧 ready A 继续作为 current，GET 仍返回 A 的 `index_summary`。
- ask／impact 不再以 `Project.status == ready` 为唯一条件，而是验证 current Snapshot 属于 Project、状态 ready、Commit object 可用且独立 Artifact bundle 完整有效；每个 AgentRun 明确绑定实际使用的 Snapshot。因此 B 失败而 A 仍 current 时，ask／impact 会 checkout exact A 并继续服务。
- Artifact 保存于受控 Project 目录下的 `artifacts/<snapshot_id>/`，包含 `index.json`、`code_graph.json` 和带 Project／Snapshot／Commit／统计值／SHA-256 的 manifest；数据库只保存相对路径和统计元数据。A/B bundle 相互独立，不会被下一次分析覆盖。
- 发布先在同一 Artifact root 的唯一 staging 目录完整构建和验证，再将旧 final 重命名为唯一 backup、将 staging 重命名为 final；第二次 rename 失败会恢复 backup。该实现不使用 `os.replace` 覆盖 Windows 非空目录，残留 backup 可在后续操作恢复或安全清理。
- 如果 Artifact 已发布而数据库激活失败，Snapshot 留为 failed；下次相同 Commit 索引会校验已发布 bundle 并完成数据库激活，不重复运行 Core。
- Phase 3 当前 Snapshot 的 `repo/.pclens` 在首次 index／ask 使用时校验 HEAD、内容和统计值后 lazy archive，并建立 Commit 保护 ref。非 current 的 Phase 3 历史行不会在首次 Phase 4 操作中被无谓改写；其 Commit 再次成为目标时按现有唯一 Snapshot 行执行可恢复 repair。
- 数据库事务保持短小，不跨越 Clone／Fetch／Checkout／Index／Graph／Artifact IO；同一 Project 的完整流程仍由 `ProjectOperationLocks` 单进程锁保护，不同 Project 可并行。

### P4-06：失败语义与恢复结果

| 失败点 | 新目标 Snapshot | Project | 原 current ready Snapshot |
|---|---|---|---|
| Clone／Fetch／branch resolve | 尚未创建 | failed + 安全 last_error | 保留且仍可用于 ask／impact |
| Checkout／Commit／HEAD 校验 | failed | failed + 安全 last_error | 若目标为新 Commit则保留；目标即 current repair 时清除无效 current |
| Index／Graph／Artifact publish | failed，不保存半完成统计 | failed + 安全 last_error | 新 Commit失败不影响旧 current |
| Artifact 已发布、DB 激活失败 | 数据库保持 failed，可重试 | failed | 新 Commit场景仍保留旧 current；重试复用完整 bundle |
| current Artifact 缺失／损坏或 Commit 不可用 | current Snapshot 标 failed | failed，current 清空 | 不再作为有效分析版本；重新 index 可 repair |

所有返回客户端的错误继续隐藏 SQL、连接串、凭据、Git stderr、源文件内容和内部 Stack Trace；内部日志保留异常链供诊断。

### P4-07：自动化测试与回归（已完成）

新增和更新的关键测试覆盖：

- 本地 working repository + bare Git remote 的首次 Clone、默认 branch、Fetch 新 Commit、detached exact checkout、A/B Snapshot 与 Artifact 历史；
- 显式 branch、branch 不存在后重试、Fetch 失败、unsafe Git tree、命令参数和隔离环境；
- 旧 shallow Clone 执行 unshallow 后保留旧 Commit object；
- 相同 ready Commit + 有效 Artifact 跳过 AST Index／Graph，不创建新 Snapshot；
- B 的 Graph 构建失败时 A 保持 current，GET summary、ask／impact 和 AgentRun 均明确绑定 A；B 重试复用同一 Snapshot 行；
- checkout 后到发布前 HEAD 被改变时拒绝发布并标记 failed；
- Artifact 缺失／损坏 repair、Windows 非空目录 replacement 失败回滚、A/B bundle 不覆盖、Artifact 发布后数据库激活失败恢复；
- Phase 3 current legacy Artifact lazy archive，且不改写其他历史行；
- PostgreSQL `0001 → 0002 → 0001 → 0002`、backfill、CHECK、FK、跨 Project current 拒绝、事务 rollback、FastAPI 重启持久化；
- Phase 1／2／3 Core、CLI、Backend API 完整回归。

最终执行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/backend -q -rs
.\.venv\Scripts\python.exe -m pytest tests -q -rs
```

结果（首次 Phase 4 自动验收）：Backend `129 passed / 0 failed / 3 skipped`；完整项目 `354 passed / 0 failed / 3 skipped`。后续人工验收修复使最终数字更新为 P4-08 记录的 `131 passed` 和 `356 passed`。3 个 skip 均因当前 Windows 账户无创建 symlink 权限；Git tree mode、resolved-path、reparse point 和 Windows replacement 可运行测试继续覆盖对应安全边界。唯一 warning 为既有 Starlette TestClient 的 HTTPX2 弃用提示。

### P4-08：人工验收 Git 网络环境与失败可观测性修复（已完成）

首次 Repository Index／Refresh 人工验收出现间歇性 `repository_failed`。外部手工 Git 已确认 Repository、branch、Commit、full clone、remote tracking ref、tree 和 detached checkout 均正常；失败请求结束后没有保留 Git stage／stderr，因此不能把某一个历史 Git 命令或 Proxy／CA 缺失表述为已经 100% 证明的唯一根因。

代码审查确认了两项实现问题：

1. `_run()` 只继承最小 OS 环境，丢弃了 Backend 运行环境中的 Proxy／CA 设置；这是手工 Git 成功而隔离 Git 失败的最高概率原因。
2. subprocess 已捕获 returncode／stdout／stderr，但旧实现只映射通用 BackendError，没有安全记录失败 stage 和 stderr，导致 Uvicorn 现场只有 access log，无法追溯历史失败。

最小修复内容：

- 在继续隔离 HOME、Git system/global config、credential helper、hook、attribute、protocol 和 submodule 行为的前提下，只额外允许传递 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY`、`NO_PROXY` 及其小写形式，以及 `SSL_CERT_FILE`、`SSL_CERT_DIR`、`GIT_SSL_CAINFO`。
- 不允许继承 `GIT_CONFIG_COUNT`、`GIT_CONFIG_KEY_*`、`GIT_CONFIG_VALUE_*`、`GIT_PROXY_COMMAND`、`GIT_SSL_NO_VERIFY`；其他环境变量仍不在白名单中。
- 未允许 `CURL_CA_BUNDLE`：它是 curl 命令环境入口，不是当前 Git/libcurl 集成的文档化 Git TLS 控制；现有 `GIT_SSL_CAINFO` 和 OpenSSL `SSL_CERT_FILE／DIR` 已覆盖所需最小能力。
- Proxy／CA 值只传入 Git subprocess，不进入日志、Project.last_error 或 API Response。
- 为 Git 调用加入 `resolve_remote_branch`、`detect_shallow`、`clone_repository`、`unshallow_repository`、`fetch_remote_branch`、`resolve_remote_commit`、`validate_commit_tree`、`protect_snapshot_ref`、`checkout_commit`、`clean_workspace`、`verify_head` 等明确 stage。
- Git 失败通过 Uvicorn error logger 记录 project_id、stage、operation、经过校验的 remote host／branch、returncode、timeout 和最长 2048 字符的脱敏 stderr；不记录 subprocess environment 或完整 URL。URL、Authorization、Proxy-Authorization、Bearer、token/password 参数和常见 GitHub token 格式均会脱敏。
- API 和 Project.last_error 继续使用原有通用安全错误，不向客户端公开 Git stderr。
- 没有增加任何自动 retry，避免掩盖真实失败。

新增回归验证：

- 环境白名单测试确认 HTTPS_PROXY、NO_PROXY、SSL_CERT_FILE、GIT_SSL_CAINFO 进入 Git subprocess；危险 Git 配置变量和 CURL_CA_BUNDLE 不进入。
- 日志测试使用包含 proxy user/password、token、Authorization 和超长内容的模拟 stderr，确认 stage／returncode 可见，而所有敏感值均不出现并正确截断。
- 新增本地 bare remote 连续生命周期：Commit A 第一次 `/index` 成功；push B 后第一次 `/index` 成功；push C 后第一次 `/index` 成功。每一步均验证 `origin/main == HEAD == current Snapshot.commit_sha == expected Commit`，protected Snapshot ref 一致，且 A/B/C 三套 Snapshot Artifact 独立保留。

实际执行与结果：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/backend/test_repositories.py::test_git_network_environment_is_allowlisted_and_failure_log_is_safe tests/backend/test_phase4_lifecycle.py::test_first_attempt_a_b_c_refreshes_keep_git_snapshot_and_artifacts_aligned -q
# 2 passed

.\.venv\Scripts\python.exe -m pytest tests/backend/test_repositories.py tests/backend/test_phase4_lifecycle.py tests/backend/test_artifacts.py -q -rs
# 49 passed

.\.venv\Scripts\python.exe -m pytest tests/backend -q -rs
# 131 passed, 3 skipped, 0 failed

.\.venv\Scripts\python.exe -m pytest tests -q -rs
# 356 passed, 3 skipped, 0 failed
```

3 个 skip 仍为当前 Windows 账户无 symlink 创建权限；唯一 warning 仍为既有 Starlette TestClient HTTPX2 弃用提示。本次没有修改 Schema／Migration 或数据库，没有执行真实 Project `/index`，也没有 Git commit／push。

#### 人工验收结果

真实 GitHub HTTPS 验收时发现 Git subprocess 环境白名单未继承 Proxy/CA，
导致 Backend Git 与手工 Git 网络环境不一致。

修复：
- 受控继承 Proxy/CA
- 保持危险 Git 配置隔离
- 增加 Repository operation stage 安全日志
- 不引入自动 retry

回归：
A → B → C 连续首次 refresh 均成功。

## 3. 用户手动复测的最少步骤

以下步骤只需要一个**专用、公开、可由验收者更新的 HTTPS Git 测试仓库**。不要使用包含敏感代码的仓库，不要在 Repository URL 中嵌入 Token；Backend 不会执行仓库代码或安装仓库依赖。

### 3.1 前置检查

确认 `.env` 中 `DATABASE_URL` 指向 `pycode_dev`、`TEST_DATABASE_URL` 指向独立的 `pycode_test`，且两者不相同。先在项目根目录执行：

```powershell
.\.venv\Scripts\python.exe -m alembic current
.\.venv\Scripts\python.exe -m alembic check
.\.venv\Scripts\python.exe -m pytest tests/backend/test_phase4_migration.py tests/backend/test_artifacts.py tests/backend/test_phase4_lifecycle.py -q -rs
```

通过标准：

- Alembic 显示 `20260910_0002 (head)`；
- Alembic check 显示 `No new upgrade operations detected.`；
- 三个定向测试文件为 0 failed；
- Migration 测试只允许操作 `pycode_test`，会清理其中四张业务表的测试数据，但不会 DROP DATABASE／DROP SCHEMA，也不会连接 `pycode_dev` 执行 downgrade。

### 3.2 准备 Commit A 并首次 Index

先在专用测试仓库的默认 branch 提交并推送 Commit A，至少包含一个可解析的 `main.py`。在终端 A 启动 Backend：

```powershell
.\.venv\Scripts\python.exe -m uvicorn backend.app.main:app --host 127.0.0.1 --port 8000
```

在终端 B 创建 Project 并执行首次 Index；将 `$RepoUrl` 替换为测试仓库的公开 HTTPS Clone URL：

```powershell
$Api = "http://127.0.0.1:8000/api/v1/projects"
$RepoUrl = "https://github.com/<owner>/<phase4-test-repository>.git"
$Created = Invoke-RestMethod -Method Post -Uri $Api -ContentType "application/json" -Body (@{
    name = "Phase 4 Manual Acceptance"
    repo_url = $RepoUrl
} | ConvertTo-Json)
$ProjectId = $Created.id
$IndexedA = Invoke-RestMethod -Method Post -Uri "$Api/$ProjectId/index"
$IndexedA | ConvertTo-Json -Depth 5
```

通过标准：返回 `status = ready`、`last_error = null` 且 `index_summary` 三个统计值存在。使用 pgAdmin 或其他 PostgreSQL 客户端执行：

```sql
SELECT p.id, p.status, p.workspace_path, p.current_snapshot_id,
       s.commit_sha, s.status AS snapshot_status,
       s.index_artifact_path, s.graph_artifact_path,
       s.file_count, s.node_count, s.edge_count
FROM projects p
LEFT JOIN project_snapshots s ON s.id = p.current_snapshot_id
WHERE p.id = '<ProjectId>';
```

通过标准：`current_snapshot_id` 非空并指向 ready Snapshot；`commit_sha` 等于远程 Commit A；两个 Artifact path 形如 `artifacts/<snapshot-id>/...`。`workspace_path` 指向 `repo`，其父目录下对应 Artifact 文件和 `manifest.json` 均存在。

### 3.3 验证相同 Commit 幂等

远程 branch 仍保持 Commit A，不做新提交，记录上一条 SQL 返回的 Snapshot ID 和 `updated_at`，再次执行：

```powershell
$RepeatedA = Invoke-RestMethod -Method Post -Uri "$Api/$ProjectId/index"
$RepeatedA | ConvertTo-Json -Depth 5
```

随后查询：

```sql
SELECT id, commit_sha, status, updated_at
FROM project_snapshots
WHERE project_id = '<ProjectId>'
ORDER BY created_at;
```

通过标准：仍然只有 Commit A 的一个 Snapshot，Snapshot ID 和 `updated_at` 不变，Artifact 目录未新增；请求返回 ready。该结果证明 ready Snapshot 与完整 Artifact 被直接复用，没有重复创建分析版本。

### 3.4 推送 Commit B 并验证历史保留

在测试仓库增加一个普通 Python 文件或修改 `main.py`，提交并推送 Commit B，然后再次调用：

```powershell
$IndexedB = Invoke-RestMethod -Method Post -Uri "$Api/$ProjectId/index"
$IndexedB | ConvertTo-Json -Depth 5
```

执行：

```sql
SELECT id, commit_sha, status, index_artifact_path, graph_artifact_path,
       file_count, node_count, edge_count
FROM project_snapshots
WHERE project_id = '<ProjectId>'
ORDER BY created_at;

SELECT current_snapshot_id
FROM projects
WHERE id = '<ProjectId>';
```

通过标准：存在 A、B 两个不同 Commit 的 ready Snapshot；Project current 指向 B；A/B 的 Artifact 目录不同且两套 `index.json`、`code_graph.json`、`manifest.json` 都仍存在。受控 Repository 中 `git rev-parse HEAD` 应等于 Commit B，`git symbolic-ref -q HEAD` 应无 branch 输出，表示 detached HEAD。

### 3.5 验证新版本失败时旧版本仍可用

在测试仓库提交并推送 Commit C，其中加入明确的 Python 语法错误，例如 `broken.py` 内容为 `def broken(:`，然后调用 Index：

```powershell
try {
    Invoke-RestMethod -Method Post -Uri "$Api/$ProjectId/index"
} catch {
    $_.ErrorDetails.Message
}
$AfterFailedC = Invoke-RestMethod -Method Get -Uri "$Api/$ProjectId"
$AfterFailedC | ConvertTo-Json -Depth 5
```

通过标准：Index 返回 `invalid_source`，Project 显示 `status = failed` 且 `last_error` 为安全错误；数据库中 C 对应 Snapshot 为 failed，但 `projects.current_snapshot_id` 仍指向 ready Snapshot B，GET 返回的 `index_summary` 仍是 B 的统计信息。

若本地已配置可用模型，再执行最少一次 ask 和 impact：

```powershell
$Ask = Invoke-RestMethod -Method Post -Uri "$Api/$ProjectId/ask" -ContentType "application/json" -Body (@{
    question = "当前代码入口是什么？"
} | ConvertTo-Json)
$Impact = Invoke-RestMethod -Method Post -Uri "$Api/$ProjectId/impact" -ContentType "application/json" -Body (@{
    file_path = "main.py"
} | ConvertTo-Json)
```

然后核验：

```sql
SELECT id, run_type, status, snapshot_id, created_at
FROM agent_runs
WHERE project_id = '<ProjectId>'
ORDER BY created_at DESC;
```

通过标准：即使 Project 因 C 更新失败而处于 failed，ask／impact 仍可执行，新增 AgentRun 的 `snapshot_id` 均等于 B。如果模型配置不可用，请求可以因模型错误失败，但 AgentRun 仍必须绑定 B；模型回答质量不属于 Phase 4 验收范围。

### 3.6 修复远程代码并验证重启恢复

修复语法错误，提交并推送新的 Commit D，再调用一次 Index，确认创建 D 的 ready Snapshot并把 current 切换到 D。随后停止并重新启动 Uvicorn，不要删除 Workspace，再使用原 `$ProjectId` 执行：

```powershell
$Restored = Invoke-RestMethod -Method Get -Uri "$Api/$ProjectId"
$Restored | ConvertTo-Json -Depth 5
Invoke-RestMethod -Method Post -Uri "$Api/$ProjectId/index"
```

通过标准：重启后原 Project 仍可查询，`index_summary` 存在；current 仍指向 D；重复 Index 不新增 D 的重复 Snapshot。若模型可用，再调用 ask／impact，应产生绑定 D 的 AgentRun。

完成上述六步即可覆盖 Phase 4 最核心的人工验收链路：首次 Clone、Fetch 更新、exact Commit、Snapshot 历史、Artifact 历史、同 Commit 幂等、新版本失败回退、AgentRun 版本绑定及 FastAPI 重启恢复。无需人工制造 Windows rename 故障或修改数据库约束，这两部分由定向自动化测试覆盖。

## 4. Phase 4 完成项

- [x] Clone／Fetch／默认或显式 branch 解析使用安全、非交互 Git 环境
- [x] 新 Workspace 使用完整所选 branch 历史，旧 shallow Workspace 自动 unshallow
- [x] 每次分析绑定 exact Commit，detached checkout 前后具有 HEAD 一致性防护
- [x] `(project_id, commit_sha)` 幂等，ready + 有效 Artifact 不重复分析
- [x] Snapshot A/B 历史和各自 Index／Graph Artifact 同时保留
- [x] Snapshot indexing／ready／failed 状态和错误信息持久化
- [x] `current_snapshot_id` 正确表达当前可用分析版本
- [x] 新 Snapshot 失败不会破坏旧 ready current Snapshot
- [x] ask／impact 与 AgentRun 使用并绑定经过完整验证的 current Snapshot
- [x] Windows staging／backup／replacement repair 可恢复且有自动测试
- [x] Phase 3 Schema 与 Artifact 兼容，不修改 `20260910_0001`
- [x] `20260910_0002` 已在 `pycode_test` 验证并升级到 `pycode_dev`
- [x] Alembic current／check、Backend 测试和完整 pytest 回归通过
- [x] 未新增无必要 HTTP API，未提前引入 Phase 5 或后续基础设施

## 5. 阶段边界

本阶段没有引入 Redis、Celery、Worker、后台队列、SSE、WebSocket、React、Vector Database、Neo4j、Multi-Agent、Commit Compare 产品 API/UI、GitHub Webhook/App、自动依赖安装或 Repository Test 执行。总计划 Current Phase 保持 Phase 4，等待人工验收后再决定是否进入 Phase 5。

---

# Phase 5A — Redis + Celery + Worker 基础设施

日期：2026-09-11。状态：代码实现、自动化验收和真实 Redis + Worker 端到端人工验收均已完成。范围只包含可持久化后台任务、Redis/Celery producer/worker 基础设施、health 验证任务与最小查询 API；该阶段 Repository Indexing 和 Agent Run 仍保持 Phase 4 的同步行为。

## 1. 计划开发内容

- 新增独立的 `background_tasks` 执行过程模型，统一只暴露 queued／running／completed／failed，不暴露 Celery 内部状态。
- 通过现有 UnitOfWork/Repository 同时提供 PostgreSQL 实现和不依赖 Redis 的内存测试实现。
- Redis 只作为 Celery broker；PostgreSQL 是 BackgroundTask 状态的最终事实来源，不配置 Celery result backend。
- 新增可注入 dispatcher、薄 Celery task adapter、独立 Worker 入口、health background task，以及 POST health／GET task 最小 API。
- 不迁移 index／ask／impact，不实现 retry、timeout、幂等、防重、分布式锁、进度、SSE 或 Worker 恢复。

## 2. 当前实施记录

### P5A-01：BackgroundTask Schema、领域状态机与最小链路（已完成）

- 新增 `BackgroundTask` 领域对象和 `BackgroundTaskStatus`，状态限定为 queued／running／completed／failed。
- 新增 `BackgroundTaskORM` 和线性 Alembic Revision `20260911_0003`；Revision 只创建 `background_tasks`，不修改 Phase 3／4 历史 Revision 和四张既有业务表。
- 新增 BackgroundTask Repository protocol、内存实现与 SQLAlchemy 实现，并接入现有 UnitOfWork。状态转换由 Repository 校验：创建为 queued；Worker 开始时进入 running 并增加 attempt_count；之后只能进入 completed 或 failed。
- 新增 `BackgroundTaskService`。创建逻辑先提交 PostgreSQL queued 行再 enqueue；执行逻辑在耗时操作前提交 running，在结束后单独提交 completed／failed，避免长事务跨越 Worker 工作。
- 新增 `REDIS_URL` 配置校验、Celery app factory、lazy producer dispatcher、独立 `backend.app.worker:app` 入口和薄 `pycode.tasks.health` adapter。Celery 使用 Redis broker、JSON serializer、UTC 和 `pycode` queue，关闭 result backend 并忽略 result。
- 新增 `POST /api/v1/tasks/health`（202）和 `GET /api/v1/tasks/{task_id}`。生产仍默认 PostgreSQL；测试可注入内存 UoW 与 recording dispatcher，因此 unit/API 测试不需要真实 Redis。
- Celery 5.6.3 与 redis-py 6.4.0 已按 `pyproject.toml` 的 backend extra 安装到项目虚拟环境。

定向测试：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/backend/test_background_tasks.py -q --tb=short -x
# 10 passed
```

该批测试覆盖 queued 创建、running 可观测、completed、异常 failed、安全错误持久化、非法重复执行、enqueue 失败 503 与日志凭据保护、POST/GET/404/422、Celery producer 参数和薄 task adapter。未连接真实 Redis 或 Worker。

### P5A-02：PostgreSQL Migration 与 Repository 验证（已完成）

- 在安全检查确认 `TEST_DATABASE_URL` 指向 `pycode_test` 且不等于开发库后，执行 `20260910_0002 → 20260911_0003 → 20260910_0002 → head`。
- 验证 upgrade 只新增 `background_tasks`，downgrade 只删除该表；12 个字段和 project／snapshot／agent_run 三个 nullable FK 均与 ORM 一致。
- PostgreSQL Repository 验证 queued → running → completed、running → failed、attempt_count、错误字段、跨 Session 持久化与无效 FK rollback。
- 测试库最终恢复到 `20260911_0003 (head)`。开发库随后执行加法 migration 从 `20260910_0002` 升级到 `20260911_0003`，没有 drop 或改写 Phase 4 表。
- 开发库最终 `alembic current` 为 `20260911_0003 (head)`，`alembic check` 为 `No new upgrade operations detected.`。

### P5A-03：Worker 装载与完整回归（已完成）

- Worker app 在仅提供测试 Redis URL、但不连接 Redis 的装载检查中成功注册 `pycode.tasks.health`。
- Backend 完整回归：`144 passed / 3 skipped / 0 failed`。
- 全项目完整回归：`369 passed / 3 skipped / 0 failed`。
- 3 个 skip 与 Phase 4 相同，均因当前 Windows 账户没有创建 symlink 的权限；其他 Git tree mode、resolved path、reparse point 和 Artifact replacement 边界测试仍运行。
- 唯一 warning 是既有 Starlette TestClient 的 HTTPX2 弃用提示。
- 沙箱内直接运行依赖 `tmp_path` 的测试会因无法访问系统 `pytest-of-hp-pc` 目录而报 `WinError 5`；在获准的正常 Windows 测试环境中复跑后全部通过，该问题不是产品代码失败。

实际执行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/backend/test_phase5a_migration.py tests/backend/test_postgresql.py -q -rs --tb=short
# 8 passed；另 1 个 tmp_path 用例因沙箱临时目录 ACL 未进入执行

.\.venv\Scripts\python.exe -m pytest tests/backend -q -rs --tb=short
# 144 passed, 3 skipped, 0 failed

.\.venv\Scripts\python.exe -m pytest tests -q -rs --tb=short
# 369 passed, 3 skipped, 0 failed
```

## 3. 实际完成情况

| 层次 | Phase 5A 结果 |
|---|---|
| Domain | 独立 BackgroundTask 与四态业务状态；不复用或混淆 AgentRun／Snapshot |
| Persistence | PostgreSQL `background_tasks` + Repository/UoW；Redis 不保存唯一业务事实 |
| Application | Service 负责创建、查询和生命周期；长操作不跨数据库事务 |
| Queue Adapter | Redis broker + Celery producer；不配置 result backend，不读取 Celery 状态作为 API 状态 |
| Worker | 独立进程入口、每次任务独立 SQLAlchemy Session、薄 health task adapter |
| HTTP | `POST /api/v1/tasks/health` 与 `GET /api/v1/tasks/{task_id}` |
| Testability | 内存 Repository、可注入 dispatcher、mock Celery app；普通 unit test 无需 Redis |
| Phase 4 兼容 | index／ask／impact 未迁移，Repository／Commit／Snapshot／Artifact 逻辑未修改 |

## 4. 真实 Redis + Worker 手动验收

本机 `.env` 在自动验收时未配置 `REDIS_URL`，因此自动测试没有伪装成真实 broker 端到端测试。完成下面步骤才能验证真实 FastAPI → PostgreSQL → Redis → Celery Worker → PostgreSQL 链路。

1. 启动可访问的 Redis，并在 `.env` 配置：

```dotenv
REDIS_URL=redis://127.0.0.1:6379/0
```

2. 确认 `DATABASE_URL` 指向开发库并执行 migration：

```powershell
.\.venv\Scripts\python.exe -m alembic upgrade head
.\.venv\Scripts\python.exe -m alembic current
```

3. 终端 A 启动 FastAPI：

```powershell
.\.venv\Scripts\python.exe -m uvicorn backend.app.main:app --host 127.0.0.1 --port 8000
```

4. 终端 B 启动 Worker；Windows 必须使用 `solo` pool：

```powershell
.\.venv\Scripts\python.exe -m celery -A backend.app.worker:app worker --loglevel=INFO --pool=solo
```

5. 终端 C 创建任务并轮询：

```powershell
$Base = "http://127.0.0.1:8000"
$Task = Invoke-RestMethod -Method Post -Uri "$Base/api/v1/tasks/health"
$Task
do {
    Start-Sleep -Milliseconds 250
    $Current = Invoke-RestMethod -Uri "$Base/api/v1/tasks/$($Task.id)"
    $Current
} while ($Current.status -in @("queued", "running"))
```

通过标准：POST 返回 202 和 queued；Worker 日志显示收到 `pycode.tasks.health`；GET 最终返回 completed、attempt_count=1、started_at／finished_at 非空、错误字段为空。数据库中同一行应为 completed。异常 → failed 与安全 error_code／error_message 由自动化 service 测试覆盖；Phase 5A 的公开 health endpoint 不提供故意制造失败的参数。

## 5. 已知边界与 Phase 5B 前置决策

- PostgreSQL commit 与 Redis enqueue 不是一个原子事务。当前先持久化 queued 再 enqueue；broker 发送失败时 API 返回安全的 `503 task_queue_unavailable`，queued 行保留供诊断。Outbox、补偿扫描和自动恢复不属于 5A。
- Worker 在成功标记 running 后崩溃会留下 running；如何识别 stale task、是否结合 Celery acknowledgement／visibility timeout 恢复，应在 5B 明确定义。
- Retry、timeout、幂等键、防重复任务、Redis lock、进度、SSE/WebSocket 均未实现。
- Phase 5B 迁移 Repository Indexing 前，需要确认 HTTP 兼容策略、BackgroundTask 与 ProjectSnapshot 的失败映射、同 Project 并发策略，以及旧 current ready Snapshot 的异步失败回退规则；Phase 4 语义应继续作为约束而非重新设计。

---

# Phase 5B — Repository Indexing 异步化

日期：2026-09-13。状态：代码实现与自动化回归完成，等待真实 Redis + Worker Repository Indexing 人工验收。本阶段只迁移 Repository Indexing；Agent Run、retry、timeout、幂等、防重、分布式锁、Outbox、崩溃恢复和事件流均未实现。

## 1. P5B-01：HTTP 202 提交链（已完成）

- `POST /api/v1/projects/{project_id}/index` 改为 `202 Accepted`，返回 `task_id`、`project_id` 和 `status=queued`。
- `BackgroundTaskService.create_repository_index_task()` 在同一数据库事务中验证 Project 并创建 `task_type=repository_index` 的 queued 记录；事务提交后才 enqueue。
- FastAPI Router 不再依赖或调用 `ProjectService.index()`。enqueue 失败保持 Phase 5A 策略：返回 `503 task_queue_unavailable`，已提交的 queued 记录保留供诊断。

## 2. P5B-02：Worker 执行链与 Snapshot 关联（已完成）

- Celery 注册稳定业务 task name `pycode.tasks.index_repository`；adapter 只解析 BackgroundTask UUID 并调用 Worker application service，不包含 Git、Commit、Snapshot、Artifact 或 Code Graph 逻辑。
- Worker 为任务创建 SQLAlchemy UnitOfWork，并通过 FastAPI／Worker 共用的 composition helper 构建现有 `ProjectService`。
- `BackgroundTaskService.execute_repository_index()` 先提交 queued → running 和 `attempt_count + 1`，再调用原有 `ProjectService.index(project_id)`；成功后把 ready `current_snapshot_id` 关联到 BackgroundTask 并提交 completed。
- 异常时复用业务错误码映射，保存安全的 `error_code`／`error_message`／`finished_at` 后重新抛出，让 Celery 记录基础设施失败；Python traceback 只进入日志。
- BackgroundTask 状态与 Project／Snapshot 状态保持独立；PostgreSQL 仍是唯一业务状态事实来源，Celery result backend 仍关闭。

## 3. P5B-03：两阶段测试迁移（已完成）

- 测试 recording dispatcher 只记录 enqueue，不连接 Redis。
- 测试 inline worker 显式执行生产同一条 `BackgroundTaskService.execute_repository_index() → ProjectService.index()` 路径，没有测试专用 indexing 实现，也不会进入生产 API dependency。
- 原同步 Index API 测试已迁移为 `POST 202/queued → inline worker → GET Project/Task → Phase 4 断言`。
- 新增目标测试覆盖：HTTP 不调用 `ProjectService.index()`、repository_index 类型、queued 初态、成功/失败生命周期、attempt_count、snapshot_id、Artifact/Graph、404、enqueue 503 保留 queued、薄 Celery adapter、task 注册及 Phase 5A health 回归。

实际执行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/backend/test_repository_index_tasks.py -q -p no:cacheprovider
# 9 passed

.\.venv\Scripts\python.exe -m pytest tests/backend/test_background_tasks.py tests/backend/test_repository_index_tasks.py -q -p no:cacheprovider
# 19 passed

.\.venv\Scripts\python.exe -m pytest tests/backend/test_phase4_lifecycle.py -q -p no:cacheprovider
# 4 passed

.\.venv\Scripts\python.exe -m pytest tests/backend/test_repositories.py -q -p no:cacheprovider
# 42 passed

.\.venv\Scripts\python.exe -m pytest tests/backend -vv --tb=short -p no:cacheprovider
# 153 passed, 3 skipped, 0 failed

.\.venv\Scripts\python.exe -m pytest tests -vv --tb=short -p no:cacheprovider
# 378 passed, 3 skipped, 0 failed
```

3 个 skip 仍是 Windows 当前账户缺少 symlink 创建权限；唯一 warning 仍是既有 Starlette TestClient HTTPX2 弃用提示。PostgreSQL 集成文件实际执行并 `8 passed`。首次 backend `-q` 执行因输出缓冲在有限等待窗口被主动终止；随后所有文件分组通过，并以 `-vv` 完成整套回归，确认不存在卡死测试。

## 4. Phase 5B 架构边界

- 生产 FastAPI：只验证 Project、创建 queued BackgroundTask、enqueue 并返回 202。
- 生产 Worker：Celery adapter → BackgroundTask application service → 原有 `ProjectService.index()`。
- Phase 4 Repository preparation、exact Commit、Snapshot、Artifact 和 Code Graph 行为未复制、未重新设计。
- 不提供 retry、timeout、Idempotency-Key、duplicate prevention、Redis lock、Transactional Outbox、stale running recovery、Worker crash recovery、multi-worker 策略、Agent Run 异步化、SSE/WebSocket 或 Docker Compose。
